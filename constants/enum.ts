export enum MainLocaleKeys {
  APP = "app",
  WELCOME = "welcome",
  LOGIN = "login",
  SIGNUP = "signup",
  HOME = "home",
  PROFILE = "profile",
  SETTINGS = "settings",
}

export enum Language {
  EN = "en",
  VI = "vi",
}
